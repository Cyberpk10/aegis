[project]
name = "aegis-backend"
version = "0.1.0"
description = "Aegis defensive email-security analysis API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",
    "python-multipart>=0.0.9",
    "pyyaml>=6.0",
    "anthropic>=0.40",
    "python-dotenv>=1.0",
    "sqlalchemy>=2.0",
    "alembic>=1.13",
    "psycopg[binary]>=3.1",
    "reportlab>=4.0",
    "argon2-cffi>=23.1",
    "pyjwt>=2.9",
    "slowapi>=0.1.9",
    "httpx>=0.27",
    "msal>=1.28",
    # M3 — optional ML classifier signal (app.ml.classifier), inference only. scipy is
    # scikit-learn's transitive dependency but used directly here (sparse.hstack) so it's
    # pinned explicitly.
    "scikit-learn>=1.5",
    "scipy>=1.13",
    "joblib>=1.4",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "respx>=0.21",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["app*"]
